# Tabslate prototypes

Working copies of prototypes, one folder per id. Open a folder in Tabslate
(`src/prototypes/<id>/`) to keep building. This repo is not the Studio and
does not include the design system — clone Tabslate for that.

Pushed from Studio via **Add to Tabslate**. Pulled via **Get from Tabslate**.
